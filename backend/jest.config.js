module.exports = {
    testEnvironment: 'node',
    setupFilesAfterEnv: ['./jest.setup.js'], // Optional: for setting up global variables or configurations
    testTimeout: 10000, // Increase timeout to 10 seconds
};